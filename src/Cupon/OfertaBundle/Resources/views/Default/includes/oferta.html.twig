{#Código comun entre oferta del dia y detalle de la oferta para incluir con include#}
<section class="descripcion">
    <h1><a href="{{ path('ofertas', { 'ciudad': oferta.ciudad.slug, 'slug' : oferta.slug }) }}">{{ oferta.nombre }}</a></h1> {{ oferta.descripcion }}
    <a class="boton" href="#">Comprar</a>
</section>
<section class="galeria">
    <img alt="..." src="{{ asset('bundles/oferta/images/' ~ oferta.rutaFoto) }}">
    <p class="precio">{{ oferta.precio }} &euro; <span>{{ oferta.descuento }}</span></p>
    <p><strong>Condiciones:</strong> {{ oferta.condiciones }}</p>
</section>
{# ... #}
<section class="direccion">
    <h2>Disfruta de la oferta en</h2>
    <p>
        <a href="#">{{ oferta.tienda.nombre }}</a> {{ oferta.tienda.direccion }}
    </p>
</section>
<section class="tienda">
    <h2>Sobre la tienda</h2> {{ oferta.tienda.description }}
</section>